---
description: Mastodon agent — product submodule + Cursor charter
globs:
  - products/mastodon-agent/**
  - products/mastodon-agent-cursor/**
---

# mastodon-agent (Cursor)

- **Product authority:** `products/mastodon-agent/docs/SPEC.md` (submodule — tool-agnostic).
- **Cursor charter:** `products/mastodon-agent-cursor/AGENTS.md`.
- **Runtime:** `python3 products/mastodon-agent/scripts/post_status.py …` with `.env` in submodule root (never commit).
- **Edits:** commit product changes in **github.com/shahzebqazi/mastodon-agent**, then bump submodule here — do not fork duplicate scripts in the monorepo tree.
- **Artwork:** keep `products/mastodon-agent/docs/images/hero.png` unless asked to replace.
